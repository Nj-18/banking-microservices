server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/account_db
    username: root
    password: Smartnj18@

  jpa:
    hibernate:
      ddl-auto: update

resilience4j:
  circuitbreaker:
    instances:
      CUSTOMER-SERVICE:
        slidingWindowSize: 10
        failureRateThreshold: 50
      TRANSACTION-SERVICE:
        slidingWindowSize: 10
        failureRateThreshold: 50
